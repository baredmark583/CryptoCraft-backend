# Локальные зависимости
# Эти папки содержат все установленные пакеты и могут весить сотни мегабайт.
# Их не нужно хранить в Git, так как они легко восстанавливаются командой `npm install`.
/node_modules
/backend/node_modules
/admin/node_modules

# Скомпилированные файлы и артефакты сборки
# Эти папки генерируются автоматически при сборке проекта.
/dist
/backend/dist
/admin/dist
/.vite

# Логи
# Файлы логов, которые могут создаваться во время работы или отладки.
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
.pnpm-debug.log*

# Файлы переменных окружения
# В этих файлах хранятся секретные ключи, пароли и другие чувствительные данные.
# Их НИКОГДА нельзя добавлять в Git. В репозитории должен быть только пример (.env.example).
.env
.env.local
.env.*.local

# Файлы и папки IDE / редакторов кода
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Файлы операционной системы
# Системный "мусор", создаваемый macOS и Windows.
.DS_Store
Thumbs.db

# Прочее
.npm
.eslintcache